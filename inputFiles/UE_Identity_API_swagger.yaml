---
swagger: "2.0"
info:
  description: "The ETSI MEC ISG MEC014 UE Identity API described using OpenAPI"
  version: "1.1.1"
  title: "UE Identity API"
  license:
    name: "ETSI Forge copyright notice"
    url: "https://forge.etsi.org/etsi-forge-copyright-notice.txt"
host: "127.0.0.1:8081"
basePath: "/ui/v1"
schemes:
- "http"
- "https"
consumes:
- "application/json"
produces:
- "application/json"
security:
- OauthSecurity:
  - "all"
paths:
  /{appInstanceId}/ue_identity_tag_info:
    get:
      description: "Retrieves information about a specific UeIdentityTagInfo resource"
      operationId: "appInstanceIdUe_identity_tag_infoGET"
      produces:
      - "application/json"
      parameters:
      - name: "appInstanceId"
        in: "path"
        description: "Represents a mobile edge application instance"
        required: true
        type: "string"
      - name: "ueIdentityTag"
        in: "query"
        description: "Represents a UE"
        required: true
        type: "array"
        items:
          type: "string"
        collectionFormat: "multi"
      responses:
        200:
          description: "It is used to indicate nonspecific success. The response body\
            \ contains a representation of the UeIdentityTagInfo resource."
          schema:
            $ref: "#/definitions/inline_response_200"
        400:
          description: "Incorrect parameters were passed in the request.In the returned\
            \ ProblemDetails structure, the \"detail\" attribute should convey more\
            \ information about the error."
          schema:
            $ref: "#/definitions/inline_response_400"
        403:
          description: "The operation is not allowed given the current status of the\
            \ resource. More information should be provided in the \"detail\" attribute\
            \ of the \"ProblemDetails\" structure."
          schema:
            $ref: "#/definitions/inline_response_403"
        404:
          description: "The client provided a URI that cannot be mapped to a valid\
            \ resource URL. In the returned ProblemDetails structure, the \"detail\"\
            \ attribute should convey more information about the error."
          schema:
            $ref: "#/definitions/inline_response_400"
      x-swagger-router-controller: "Default"
    put:
      description: "Register/De-register the information about specific a UeIdentityTagInfo\
        \ resource"
      operationId: "appInstanceIdUe_identity_tag_infoPUT"
      produces:
      - "application/json"
      parameters:
      - name: "appInstanceId"
        in: "path"
        description: "Represents a mobile edge application instance"
        required: true
        type: "string"
      - in: "body"
        name: "UeIdentityTagInfo"
        description: "'The updated \"state\" for each included UE Identity tag is\
          \ included in the entity body of the request'"
        required: true
        schema:
          $ref: "#/definitions/UeIdentityTagInfo"
      responses:
        200:
          description: "It is used to indicate success. The response body contains\
            \ a representation of the UeIdentityTagInfo resource"
          schema:
            $ref: "#/definitions/inline_response_200"
        400:
          description: "Incorrect parameters were passed in the request.In the returned\
            \ ProblemDetails structure, the \"detail\" attribute should convey more\
            \ information about the error."
          schema:
            $ref: "#/definitions/inline_response_400"
        401:
          description: "An erroneous or missing bearer token. More information should\
            \ be provided in the \"detail\" attribute of the \"ProblemDetails\" structure."
          schema:
            $ref: "#/definitions/inline_response_400"
        403:
          description: "The operation is not allowed given the current status of the\
            \ resource. More information should be provided in the \"detail\" attribute\
            \ of the \"ProblemDetails\" structure."
          schema:
            $ref: "#/definitions/inline_response_403"
        404:
          description: "The client provided a URI that cannot be mapped to a valid\
            \ resource URL. In the returned ProblemDetails structure, the \"detail\"\
            \ attribute should convey more information about the error."
          schema:
            $ref: "#/definitions/inline_response_400"
        412:
          description: "It is used when a condition has failed during conditional\
            \ requests, e.g. when using ETags to avoid write conflicts.In the returned\
            \ ProblemDetails structure, the \"detail\" attribute should convey more\
            \ information about the error."
          schema:
            $ref: "#/definitions/inline_response_400"
      x-swagger-router-controller: "Default"
securityDefinitions:
  OauthSecurity:
    type: "oauth2"
    tokenUrl: "https://oauth.exampleAPI/token"
    flow: "application"
    scopes:
      all: "Single oauth2 scope for API"
definitions:
  ProblemDetails:
    type: "object"
    properties:
      type:
        $ref: "#/definitions/Problem.type"
      title:
        $ref: "#/definitions/Problem.title"
      status:
        $ref: "#/definitions/Problem.status"
      detail:
        $ref: "#/definitions/Problem.detail"
      instance:
        $ref: "#/definitions/Problem.instance"
  Problem.type:
    type: "string"
    format: "uri"
    description: "A URI reference according to IETF RFC 3986 that identifies the problem\
      \ type"
  Problem.title:
    type: "string"
    description: "A short, human-readable summary of the problem type"
  Problem.status:
    type: "integer"
    format: "uint32"
    description: "The HTTP status code for this occurrence of the problem"
  Problem.detail:
    type: "string"
    description: "A human-readable explanation specific to this occurrence of the\
      \ problem"
  Problem.instance:
    type: "string"
    format: "uri"
    description: "A URI reference that identifies the specific occurrence of the problem"
  UeIdentityTagInfo:
    type: "object"
    required:
    - "ueIdentityTags"
    properties:
      ueIdentityTags:
        $ref: "#/definitions/UeIdentityTags"
    description: "information of UE identity tag used in UE Identity feature"
    example:
      ueIdentityTags: ""
  UeIdentityTags:
    type: "array"
    description: "1 to N tags presented by a ME Application instance to a ME Platform"
    items:
      $ref: "#/definitions/UeIdentityTags_inner"
  UeIdentityTag:
    type: "string"
    description: "Specific tag presented by a ME Application instance to a ME Platform"
    example: "UeTagA"
  State:
    type: "string"
    description: "Status of the resource ueIdentityTagInfo"
    example: "REGISTERED"
    enum:
    - "UNREGISTERED"
    - "REGISTERED"
  inline_response_200:
    required:
    - "UeIdentityTagInfo"
    properties:
      UeIdentityTagInfo:
        $ref: "#/definitions/UeIdentityTagInfo"
    example:
      UeIdentityTagInfo:
        ueIdentityTags: ""
  inline_response_400:
    properties:
      ProblemDetails:
        $ref: "#/definitions/ProblemDetails"
  inline_response_403:
    required:
    - "ProblemDetails"
    properties:
      ProblemDetails:
        $ref: "#/definitions/ProblemDetails"
  UeIdentityTags_inner:
    required:
    - "state"
    - "ueIdentityTag"
    properties:
      ueIdentityTag:
        $ref: "#/definitions/UeIdentityTag"
      state:
        $ref: "#/definitions/State"
parameters:
  Body.UeIdentityTagInfo:
    in: "body"
    name: "UeIdentityTagInfo"
    description: "'The updated \"state\" for each included UE Identity tag is included\
      \ in the entity body of the request'"
    required: true
    schema:
      $ref: "#/definitions/UeIdentityTagInfo"
  Path.AppInstanceId:
    name: "appInstanceId"
    in: "path"
    description: "Represents a mobile edge application instance"
    required: true
    type: "string"
  Query.UeIdentityTag:
    name: "ueIdentityTag"
    in: "query"
    description: "Represents a UE"
    required: true
    type: "array"
    items:
      type: "string"
    collectionFormat: "multi"
externalDocs:
  description: "ETSI GS MEC014 UE Identity API, V1.1.1"
  url: "http://www.etsi.org/deliver/etsi_gs/MEC/001_099/014/01.01.01_60/gs_mec014v010101p.pdf"
